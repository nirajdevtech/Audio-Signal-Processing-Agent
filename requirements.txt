# ============================================================
# Audio Signal Processing Assistant - Python Dependencies
# ============================================================
# Tested with Python 3.11 on Windows, macOS, and Linux.
# All packages ship pre-built wheels — no C compiler needed.
#
# Dependency notes:
#   ibm-watsonx-ai 1.1.2 pulls ibm-cos-sdk → ibm-cos-sdk-core 2.13.5
#   ibm-cos-sdk-core 2.13.5 requires urllib3 < 2.2
#   ibm-cloud-sdk-core 3.21.0 requires urllib3 >= 2.1, < 3.0  ← compatible
#   ibm-cloud-sdk-core >= 3.22 requires urllib3 >= 2.4         ← CONFLICTS
# ============================================================

# Web Framework
Flask==3.0.3
Werkzeug==3.0.3

# Environment variable management
python-dotenv==1.0.1

# IBM Watsonx.ai SDK (foundation model inference)
ibm-watsonx-ai==1.1.2

# IBM Cloud authentication
# 3.21.0 is the highest version with a pre-built wheel that stays
# compatible with the urllib3 < 2.2 constraint from ibm-cos-sdk-core.
ibm-cloud-sdk-core==3.21.0

# Pin urllib3 to the shared-compatible range
urllib3>=2.1.0,<2.2.0

# Production WSGI server (optional, for deployment)
gunicorn==22.0.0
